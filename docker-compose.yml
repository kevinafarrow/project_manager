services:
  engagement-pm:
    build: .
    container_name: engagement-pm
    # 42688 = "GANTT" on a phone keypad — unlikely to collide with anything
    ports:
      - "42688:42688"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
